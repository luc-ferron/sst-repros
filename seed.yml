before_compile:
  - echo "Before compile"

#compile:
#  - echo "Override compile"

before_build:
  - echo "Before build"

before_deploy:
  - echo "Before deploy"
  - npx sst bind vitest run

after_deploy:
  - echo "After deploy"
  - npx sst bind vitest run

before_remove:
  - echo "Before deploy"

after_remove:
  - echo "After deploy"

