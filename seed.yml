before_compile:
  - echo "Before compile"

#compile:
#  - echo "Override compile"

before_build:
  - echo "Before build"

before_deploy:
  - echo "Before deploy"

after_deploy:
  - echo "After deploy"
  - npm install --silent && npx sst bind --stage sst-repro vitest run --verbose

before_remove:
  - echo "Before deploy"

after_remove:
  - echo "After deploy"

